{%if number==1%}
<div class="carousel-item active aspectRatio">
    <img src="{{url}}" class="d-block w-100 images aspectFoto" alt="{{number}}slide">
</div>
{%else%}
<div class="carousel-item aspectRatio">
    <img src="{{url}}" class="d-block w-100 images aspectFoto" alt="{{number}}slide">
</div>
{%endif%}
